<%= turbo_stream.prepend "#{dom_id(@post)}_comments" do %>
  <%= render "comment", comment: @comment %>
<% end %>

<%= turbo_stream.replace "#{dom_id(@post)}_comments_count" do %>
  <%= render "posts/comments_count", post: @post %>
<% end %>
